on:
  push:
    branches:
      - main
name: 🚀 Deploy website on push
jobs:
  my_job:
    runs-on: ubuntu-latest
    steps:
      - name: My Local Action
        run: |
          bash ./.github/actions/my-action/script.sh