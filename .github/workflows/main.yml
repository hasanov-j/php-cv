on:
  push:
    branches:
      - main

name: 🚀 Deploy website on push

jobs:
  my_job:
    runs-on: ubuntu-latest

    steps:
      - name: My Local Action
        run: |
            cd $GITHUB_WORKSPACE & ls
#          bash $GITHUB_WORKSPACE/.github/actions/script.sh