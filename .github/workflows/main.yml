on:
  push:
    branches:
      - main

name: 🚀 Deploy website on push

jobs:
  my_job:
    runs-on: ubuntu-latest

    steps:
      - name: Show Files
        uses: actions/checkout
      - name: My Local Action
        run: |
          pwd
          ls

